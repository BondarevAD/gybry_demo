import 'package:flutter/material.dart';

class TeamPageBody extends StatelessWidget {
  const TeamPageBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Раздел "Команда"'));
  }
}
